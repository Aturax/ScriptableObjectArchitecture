using UnityEngine;

public class Test : MonoBehaviour
{
    public FloatVariable playerHealth = null;
    public FloatList list = null;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
